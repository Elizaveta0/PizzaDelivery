@model ICollection<Pizza>
@inject ICartManager cartManager 

@{
    ViewData["Title"] = "Index";
}

@foreach (var p in Model)
{
    <a asp-action="Pizza" asp-controller="Pizza" asp-route-id="@p.Id"> @p.Name </a>
}

@if (Model.Count == 0)
{
    <p>Empty</p>
}